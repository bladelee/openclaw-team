/**
 * Empty Surface Component
 * Shown when no surfaces are available
 */

import React from 'react';

export const EmptySurface: React.FC = () => {
  return (
    <div className="a2ui-empty">
      <p>Waiting for A2UI messages...</p>
    </div>
  );
};
