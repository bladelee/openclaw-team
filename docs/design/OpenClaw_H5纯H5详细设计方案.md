# OpenClaw H5 移动端详细设计方案（纯 H5 版本）

## 目录

1. [架构概览](#架构概览)
2. [功能模块设计](#功能模块设计)
3. [UI/UX 设计](#uiux-设计)
4. [技术实现](#技术实现)
5. [数据流设计](#数据流设计)
6. [API 设计](#api-设计)
7. [安全与权限](#安全与权限)
8. [实施计划](#实施计划)

---

## 架构概览

### 整体架构

```
┌─────────────────────────────────────────────────────────────────────┐
│  OpenClaw H5 移动端架构                                                  │
├─────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │                    用户浏览器                                │  │
│  │  ┌───────────────────────────────────────────────────────────┐  │  │
│  │  │  A2UI React H5 (已有)                                      │  │  │
│  │  │  ┌─────────────────────────────────────────────────────┐  │  │  │
│  │  │  │  Canvas UI (14 个组件)                             │  │  │  │
│  │  │  │  ┌───────────────────────────────────────────────┐  │  │  │  │
│  │  │  │  │  - Button, Text, Card, Image, TextField...  │  │  │  │  │
│  │  │  │  └───────────────────────────────────────────────┘  │  │  │  │
│  │  │  └─────────────────────────────────────────────────────┘  │  │  │
│  │  │                                                         │  │  │
│  │  │  ┌─────────────────────────────────────────────────────┐  │  │  │
│  │  │  │  新增功能模块                                        │  │  │  │
│  │  │  │  ┌────────────────┬────────────────┬────────────┐  │  │  │ │
│  │  │  │  │ 设置页面    │  │ 聊天页面   │ │ 语音助手   │  │  │  │ │
│  │  │  │  │             │  │             │  │           │  │  │  │ │
│  │  │  │  │ - 设备配置   │  │ - 消息列表  │  │ - 语音输入 │  │  │  │ │
│  │  │  │  │ - 主题切换   │  │ - 消息输入  │  │ - 语音播放 │  │  │  │ │
│  │  │  │  │ - Gateway 管理│ │ - 实时通信  │  │ - 云端识别 │  │  │  │ │
│  │  │  │  └────────────────┴────────────────┴────────────┘  │  │  │ │
│  │  │  │                                                         │  │  │  │
│  │  │  │  ┌─────────────────────────────────────────────────────┐  │  │  │ │
│  │  │  │  │  原生功能模块 (Web APIs)                           │  │  │  │ │
│  │  │  │  │  ┌────────┬────────┬────────┬────────────────┐  │  │  │ │ │
│  │  │  │  │  │相机  │  │位置  │ │屏幕录制│  │  │  │  │  │ │
│  │  │  │  │  └──────┴────────┴────────┴────────────────┘  │  │  │ │ │
│  │  │  │  └─────────────────────────────────────────────────────┘  │  │  │ │
│  │  │  │                                                         │  │  │  │
│  │  │  │  ┌─────────────────────────────────────────────────────┐  │  │  │ │
│  │  │  │  │  WebSocket 连接 (Gateway)                            │  │  │  │ │
│  │  │  │  │  - 双向实时通信                                      │  │  │  │ │ │
│  │  │  │  │  │  - 消息推送                                          │  │  │  │ │ │
│  │  │  │  │  │  - 状态同步                                          │  │  │  │ │ │
│  │  │  │  │  └─────────────────────────────────────────────────────┘  │  │  │ │
│  │  │  └─────────────────────────────────────────────────────┘  │  │  │
│  └─────────────────────────────────────────────────────────────┘  │
│                                                                            │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │  Gateway (后台服务器)                                           │  │
│  │  - AI Agent 运行时                                              │  │
│  │  - WebSocket 服务器                                             │  │
│  │  - 消息路由与处理                                              │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                            │
└─────────────────────────────────────────────────────────────────────┘

核心特点：
✅ 完全基于 H5（无原生依赖）
✅ 复用已有 A2UI React H5
✅ 新增设置、聊天、语音功能
✅ 支持相机、位置、屏幕录制
```

### 功能清单

| 功能模块 | 支持情况 | 实现方式 | 优先级 |
|---------|---------|---------|--------|
| **Canvas UI** | ✅ 100% | A2UI React H5 | P0 |
| **Gateway 连接** | ✅ 100% | WebSocket | P0 |
| **相机拍照** | ✅ 100% | MediaDevices API | P0 |
| **屏幕录制** | ✅ 100% | getDisplayMedia API | P0 |
| **位置服务** | ✅ 70% | Geolocation API (无后台) | P1 |
| **设置功能** | ✅ 100% | 新增页面 | P0 |
| **聊天功能** | ✅ 100% | 新增页面 | P0 |
| **语音输入** | ✅ 90% | MediaRecorder + 云 ASR | P1 |
| **语音输出** | ✅ 95% | 云 TTS | P1 |
| **推送通知** | ⚠️ 30% | Service Worker (有限) | P2 |
| **后台运行** | ❌ 0% | 不支持 (H5 限制) | P2 |

---

## 功能模块设计

### 1. 设置页面

#### UI 布局

```
┌─────────────────────────────────────────────────────────────────┐
│  设置                                    [保存]                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  📱 设备                                                │   │
│  │  ┌─────────────────────────────────────────────────────┐ │   │
│  │  │ 设备名称: [我的 Android]                    │ │   │
│  │  │ 设备 ID:   abc-123-def                         │ │   │
│  │  └─────────────────────────────────────────────────────┘ │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  🔌 Gateway 连接                                         │   │
│  │  ┌─────────────────────────────────────────────────────┐ │   │
│  │  │ 连接状态: ● 已连接                               │ │   │
│  │  │ 服务器:   peters-mac-studio.local              │ │   │
│  │  │ 远程地址: 192.168.1.100:18789                   │ │   │
│  │  └─────────────────────────────────────────────────────┘ │   │
│  │                                                          │   │
│  │  │ [刷新连接] [断开连接]                                │   │
│  │  └─────────────────────────────────────────────────────┘ │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  🎨 外观                                               │   │
│  │  ┌─────────────────────────────────────────────────────┐ │   │
│  │  │ 主题模式:                                             │ │   │
│  │  │  ○ 自动  ○ 深色  ○ 浅色                             │ │   │
│  │  │                                                      │ │   │
│  │  └─────────────────────────────────────────────────────┘ │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  📍 位置服务                                           │   │
│  │  ┌─────────────────────────────────────────────────────┐ │   │
│  │  │ 位置权限:                                             │ │   │
│  │  │  ⚠️ 未授权  [授权位置]                             │ │   │
│  │  │                                                      │ │   │
│  │  │ 当前位置: (仅在授权后显示)                          │ │   │
│  │  │ 纬度: 35.6789°                                       │ │   │
│  │  │ 经度: 139.7123°                                      │ │   │
│  │  │ 精度: ±10m                                            │ │   │
│  │  └─────────────────────────────────────────────────────┘ │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  🎤 语音                                               │   │
│  │  ┌─────────────────────────────────────────────────────┐ │   │
│  │  │ 语音输入:                                             │ │   │
│  │  │  ● 已授权  [测试麦克风]                            │ │   │
│  │  │                                                      │ │   │
│  │  │ 语音输出:                                             │ │   │
│  │  │  ● 可用                                               │ │   │
│  │  └─────────────────────────────────────────────────────┘ │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  ℹ️ 其他                                               │   │
│  │  ┌─────────────────────────────────────────────────────┐ │   │
│  │  │ 防止休眠:                                             │ │   │
│  │  │  [关闭]                                              │ │   │
│  │  │                                                      │ │   │
│  │  │ 版本: v1.0.0                                          │ │   │
│  │  │                                                      │   │
│  │  │ [清除缓存]                                            │ │   │
│  │  └─────────────────────────────────────────────────────┘ │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  🔧 高级设置                                           │   │
│  │  ┌─────────────────────────────────────────────────────┐ │   │
│  │  │ [开发模式] [查看日志]                                │ │   │
│  │  └─────────────────────────────────────────────────────┘ │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  💬 聊天                                               │   │
│  │  ┌─────────────────────────────────────────────────────┐ │   │
│  │  │ [打开聊天]                                            │ │   │
│  │  └─────────────────────────────────────────────────────┘ │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

#### A2UI 消息定义

```typescript
// 设置页面 A2UI 消息
const SETTINGS_MESSAGES = [
  {
    surfaceUpdate: {
      surfaceId: 'settings',
      components: [
        {
          id: 'root',
          component: {
            Column: {
              children: { explicitList: ['header', 'sections', 'actions'] },
              spacing: { literalNumber: 16 }
            }
          }
        },
        // ... 具体组件定义
      ]
    }
  },
  {
    beginRendering: {
      surfaceId: 'settings',
      root: 'root'
    }
  }
];
```

### 2. 聊天页面

#### UI 布局

```
┌─────────────────────────────────────────────────────────────────┐
│  💬 聊天                                  [语音]               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  消息列表                                              │   │
│  │  ┌─────────────────────────────────────────────────────┐ │   │
│  │  │ AI (10:30):                                          │ │   │
│  │  │  今天天气怎么样？                                        │ │   │
│  │  ├─────────────────────────────────────────────────────┤ │   │
│  │  │  ┌───────────────────────────────────────────────┐ │ │ │ │
│  │  │  │ User (10:31):                                      │ │ │ │ │
│  │  │  │  ┌───────────────────────────────────────────┐  │ │ │ │ │
│  │  │  │  │ [📷] [📍] [🎤] [📄] [🔗]           │  │ │ │ │ │
│ │  │  │  │ 查看天气  查看位置  语音  文件  链接      │ │ │ │ │ │
│  │  │  │  └───────────────────────────────────────────┘  │ │ │ │ │
│  │  │  │                                                  │  │ │ │ │ │
│  │  │  │  今天阳光明媚，25°C，适合外出。                        │ │ │ │ │
│  │  │  └───────────────────────────────────────────────┘  │ │ │ │ │
│  │  └─────────────────────────────────────────────────────┘ │   │
│  │  ┌─────────────────────────────────────────────────────┐ │   │
│  │  │ AI (10:32):                                          │ │   │
│  │  │  好的，需要我帮你做什么吗？                                │ │   │
│  │  └─────────────────────────────────────────────────────┘ │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  输入框                                                │   │
│  │  ┌─────────────────────────────────────────────────────┐ │   │
│  │  │ [📷] [📍] [🎤] [📄] [🔗]  [💬]              │ │   │
│  │  │  │                                           │   │   │ │ │ │ │
│  │  │  │ 输入消息...                               │   │   │ │ │ │ │
│  │  │  │                                           │   │   │ │ │ │ │
│  │  │  │                                           │   │   │ │ │ │ │
│  │  │  │ [发送]                                      │   │   │ │ │ │ │
│  │  │  └─────────────────────────────────────────────────────┘ │   │
│  │  └─────────────────────────────────────────────────────┘ │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

#### 功能定义

**消息列表**：
- 显示 AI 和用户的消息历史
- 支持文本、图片、位置、文件附件
- 显示发送时间和状态
- 实时更新 AI 响应

**输入框**：
- 文本输入
- 多媒体附件（拍照、位置、文件、链接）
- 语音输入（按住说话）
- 快捷操作按钮

**附件功能**：
```
📷 相机：拍照或选择相册图片
📍 位置：发送当前位置
🎤 语音：录制语音消息
📄 文件：上传文档
🔗 链接：添加链接
💬 文本：快速输入
```

### 3. 语音助手页面

#### UI 布局

```
┌─────────────────────────────────────────────────────────────────┐
│  🎤 语音助手                                  [关闭]                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  对话区域                                              │   │
│  │  ┌─────────────────────────────────────────────────────┐ │   │
│  │  │  👤 You:                                          │ │   │
│  │  │  │ 帮我查一下明天北京的天气                           │ │   │
│  │  │  │                                                  │ │   │
│  │  │  │ 🤖 AI:                                          │ │   │
│  │  │  │  好的，我帮你查...                             │ │   │
│  │  │  │  明天北京晴天，15-25°C，微风。                   │ │   │
│  │  │  └─────────────────────────────────────────────────────┘ │   │
│  │  ┌─────────────────────────────────────────────────────┐ │   │
│  │  │  🎙 正在听...                                      │ │   │
│  │  │  │ ┌─────────────────────────────────────────────┐ │ │   │
│  │  │  │  │ 🎙                                            │ │ │   │ │ │
  ┌───────────────────────────────────────────────────────────┐│ │ │ │ │
│  │  已连接到 peters-mac-studio.local                    │ │ │ │ │
│  └───────────────────────────────────────────────────────────┘│ │ │ │ │
│  [断开连接] [刷新]                                        │ │ │ │ │
│  └───────────────────────────────────────────────────────────┘│ │ │ │ │
└─────────────────────────────────────────────────────────────┘   │ │ │ │
                                                            │ │ │ │
┌───────────────────────────────────────────────────────────────┐ │ │ │ │
│  🔍 Gateway 发现                                            │ │ │ │ │
│                                                            │ │ │ │
│  正在搜索本地 Gateway...                                  │ │ │ │ │
│                                                            │ │ │ │
│  ┌─────────────────────────────────────────────────────────┐   │ │ │ │
│  │  peters-mac-studio.local                          │   │ │ │ │
│  │  [连接]                                                  │   │ │ │ │
│  └─────────────────────────────────────────────────────────┘   │ │ │ │ │
│                                                            │ │ │ │
│  还发现了其他 Gateway:                                    │ │ │ │ │
│  ┌─────────────────────────────────────────────────────────┐   │ │ │ │
│  │  office-gw.local                                    │   │ │ │ │
│  │  [连接]                                                  │   │ │ │ │
│  └─────────────────────────────────────────────────────────┘   │ │ │ │ │
└─────────────────────────────────────────────────────────────┘   │ │ │ │
                                                            │ │ │ │
┌─────────────────────────────────────────────────────────────┐   │ │ │ │
│  🔧 高级设置                                                │ │ │ │ │
│                                                            │ │ │ │
│  ┌─────────────────────────────────────────────────────────┐   │ │ │ │
│  │  [开发者模式]                                            │   │ │ │ │
│  │    - 查看日志                                           │   │ │ │ │
│  │    - 调试工具                                           │   │ │ │ │
│  │    └───────────────────────────────────────────┘   │ │ │ │ │
│  └─────────────────────────────────────────────────────────┘   │ │ │ │ │
└─────────────────────────────────────────────────────────────┘   │ │ │ │
                                                            │ │ │ │
┌─────────────────────────────────────────────────────────────┐   │ │ │ │
│  ℹ️ 其他                                                   │   │ │ │ │
│                                                            │ │ │ │
│  ┌─────────────────────────────────────────────────────────┐   │ │ │ │
│  │  防止休眠: [关闭]                                        │   │ │ │ │
│  │    （标签页关闭后连接会断开）                            │   │ │ │ │
│  │                                                          │   │ │ │ │
│  │  版本: v1.0.0 (build 2025-02-04)                    │   │ │ │ │
│  │  └───────────────────────────────────────────────────┘   │ │ │ │ │
│                                                            │ │ │ │
│  ┌─────────────────────────────────────────────────────────┐   │ │ │ │
│  │  [清除缓存]                                                │   │ │ │ │
│  └─────────────────────────────────────────────────────────┘   │ │ │ │ │
│                                                            │ │ │ │
│  ┌─────────────────────────────────────────────────────────┐   │ │ │ │ │
│  │  💬 聊天                                                   │   │ │ │ │
│  │  [打开聊天]                                                │   │ │ │ │
│  └─────────────────────────────────────────────────────────┘   │ │ │ │ │
└─────────────────────────────────────────────────────────────┘   │ │ │ │
                                                            │ │ │ │
┌─────────────────────────────────────────────────────────────┐   │ │ │ │
│  🎤 语音助手                                                │   │ │ │ │
│  │  [打开语音助手]                                            │   │ │ │ │
│  └─────────────────────────────────────────────────────────┘   │ │ │ │ │
└─────────────────────────────────────────────────────────────┘   │ │ │ │
                                                            │ │ │ │
┌─────────────────────────────────────────────────────────────┐   │ │ │ │
│  📄 临时文件                                                │   │ │ │ │
│  │                                                          │   │ │ │ │ │
│  │  存储空间: 已用 2.1 GB / 5 GB                       │   │ │ │ │ │
│  │                                                          │   │ │ │ │ │ │ │
│  │  [清理文件]                                              │   │ │ │ │
│  │                                                          │   │ │ │ │ │ │ │ │
│  │  [上传文件]                                              │   │ │ │ │
│  │  └───────────────────────────────────────────────────┘   │ │ │ │ │
└─────────────────────────────────────────────────────────────┘   │ │ │ │ │
                                                            │ │ │ │
┌─────────────────────────────────────────────────────────────┐   │ │ │ │
│  🔒 登出                                                  │   │ │ │ │
│  │  [确认退出]                                                │   │ │ │ │
│  └─────────────────────────────────────────────────────────┘   │ │ │ │ │
└─────────────────────────────────────────────────────────────┘   │ │ │ │
                                                            │ │ │ │
└─────────────────────────────────────────────────────────────┘   │ │ │ │
```

---

### 实施优先级

#### Phase 1: 核心功能（P0 - 必须实现）

| 功能 | 时间 | 复杂度 | 依赖 |
|------|------|--------|------|
| 设置页面 | 1 周 | ⭐⭐ | A2UI H5 |
| 聊天页面 | 1 周 | ⭐⭐⭐ | WebSocket + 新组件 |
| WebSocket 优化 | 3 天 | ⭐⭐ | 连接管理 |

#### Phase 2: 多媒体功能（P1 - 重要功能）

| 功能 | 时间 | 复杂度 | 依赖 |
|------|------|--------|------|
| 相机拍照 | 1 周 | ⭐⭐⭐ | MediaDevices API |
| 屏幕录制 | 1 周 | ⭐⭐⭐ | getDisplayMedia API |
| 位置服务 | 1 周 | ⭐⭐ | Geolocation API |
| 文件上传 | 1 周 | ⭐⭐⭐ | fetch + FormData |

#### Phase 3: 语音功能（P1 - 重要功能）

| 功能 | 时间 | 复杂度 | 依赖 |
|------|------|--------|------|
| 语音输入 | 1 周 | ⭐⭐⭐⭐ | MediaRecorder + 云 ASR |
| 语音输出 | 1 周 | ⭐⭐⭐ | 云 TTS API |
| 语音助手页面 | 1 周 | ⭐⭐⭐ | 综合功能 |

#### Phase 4: 高级功能（P2 - 锦上添花）

| 功能 | 时间 | 复杂度 | 依赖 |
|------|------|--------|------|
| 推送通知 | 1 周 | ⭐⭐⭐ | Service Worker + Push API |
| 临时文件存储 | 1 周 | ⭐⭐ | IndexedDB |

---

## 技术实现

### 1. 设置页面实现

#### 文件结构

```
src/canvas-host/a2ui-react/
├── pages/
│   ├── settings/
│   │   ├── SettingsPage.tsx          # 设置页面主组件
│   │   ├── settingsSlice.tsx        # Redux slice
│   │   └── SettingsA2ui.tsx         # A2UI 组件
├── services/
│   ├── SettingsService.ts          # 设置服务
│   ├── LocationService.ts           # 位置服务
│   └── ThemeService.ts              # 主题服务
└── types/
    └── settings.ts                # 设置类型定义
```

#### 设置页面组件

```typescript
// src/canvas-host/a2ui-react/pages/settings/SettingsPage.tsx
import React, { useState, useEffect } from 'react';
import { useA2uiTheme } from '../../context/A2uiThemeContext';
import { useGateway } from '../../context/GatewayContext';
import { useLocation } from '../../hooks/useLocation';
import { A2uiButton } from '../../components/elements/A2uiButton';
import { A2uiText } from '../../components/elements/A2uiText';
import { A2uiCard } from '../../components/elements/A2uiCard';
import { A2uiColumn } from '../../components/elements/A2uiColumn';
import { A2uiRow } from '../../components/elements/A2uiRow';
import { A2uiTextField } from '../../components/elements/A2uiTextField';
import { A2uiImage } from '../../components/elements/A2uiImage';
import { A2uiContainer } from '../../components/elements/A2uiContainer';
import { A2uiDivider } from '../../components/elements/A2uiDivider';

interface SettingsState {
  displayName: string;
  gatewayStatus: 'connected' | 'disconnected' | 'connecting';
  gatewayName: string | null;
  remoteAddress: string | null;
  themeMode: 'auto' | 'light' | 'dark';
  locationPermission: 'granted' | 'denied' | 'prompt';
  currentLocation: {
    latitude: number;
    longitude: number;
    accuracy: number;
  } | null;
  voiceEnabled: boolean;
  preventSleep: boolean;
}

export const SettingsPage: React.FC = () => {
  const { theme, setMode } = useA2uiTheme();
  const { isConnected, serverName, remoteAddress, connect, disconnect } = useGateway();
  const { location, requestPermission } = useLocation();
  const [state, setState] = useState<SettingsState>({
    displayName: '',
    gatewayStatus: isConnected ? 'connected' : 'disconnected',
    gatewayName: serverName,
    remoteAddress: remoteAddress,
    themeMode: theme.mode,
    locationPermission: 'prompt',
    currentLocation: null,
    voiceEnabled: false,
    preventSleep: false
  });

  // 生成设置页面的 A2UI 消息
  const generateSettingsMessages = (): A2uiMessage[] => {
    return [
      {
        surfaceUpdate: {
          surfaceId: 'settings',
          components: [
            {
              id: 'root',
              component: {
                Column: {
                  children: { explicitList: ['header', 'device', 'gateway', 'appearance', 'location', 'voice', 'advanced'] },
                  spacing: { literalNumber: 16 }
                }
              }
            },
            {
              id: 'header',
              component: {
                Text: {
                  text: { literalString: '⚙️ 设置' },
                  size: 'xlarge',
                  weight: 'bold'
                }
              }
            },
            // Device section
            {
              id: 'device',
              component: {
                Card: {
                  children: { explicitList: ['deviceName', 'deviceId'] }
                }
              }
            },
            {
              id: 'deviceName',
              component: {
                TextField: {
                  label: { literalString: '设备名称' },
                  placeholder: { literalString: '我的 Android' },
                  value: { literalString: state.displayName }
                }
              }
            },
            {
              id: 'deviceId',
              component: {
                Text: {
                  text: { literalString: `设备 ID: ${state.displayName || 'abc-123'}` },
                  size: 'small',
                  usageHint: 'caption'
                }
              }
            },
            // Gateway section
            {
              id: 'gateway',
              component: {
                Card: {
                  children: { explicitList: ['status', 'serverName', 'remoteAddress', 'actions'] }
                }
              }
            },
            {
              id: 'status',
              component: {
                Text: {
                  text: { literalString: `连接状态: ${state.gatewayStatus === 'connected' ? '● 已连接' : '● 未连接'}` },
                  size: 'medium',
                  color: state.gatewayStatus === 'connected' ? '#10b981' : '#ef4444'
                }
              }
            },
            {
              id: 'serverName',
              component: {
                Text: {
                  text: { literalString: `服务器: ${state.gatewayName || '未连接'}` },
                  size: 'small',
                  usageHint: 'caption'
                }
              }
            },
            {
              id: 'remoteAddress',
              component: {
                Text: {
                  text: { literalString: `远程地址: ${state.remoteAddress || '未知'}` },
                  size: 'small',
                  usageHint: 'caption'
                }
              },
            {
              id: 'actions',
              component: {
                Row: {
                  children: { explicitList: ['refresh', 'disconnect'] },
                  spacing: { literalNumber: 8 }
                }
              }
            },
            {
              id: 'refresh',
              component: {
                Button: {
                  text: { literalString: '刷新连接' },
                  action: {
                    name: 'refresh_gateway',
                    surfaceId: 'settings',
                    context: []
                  }
                }
              }
            },
            {
              id: 'disconnect',
              component: {
                Button: {
                  text: { literalString: '断开连接' },
                  action: {
                    name: 'disconnect_gateway',
                    surfaceId: 'settings',
                    context: []
                  },
                  disabled: state.gatewayStatus !== 'connected'
                }
              }
            },
            // Theme section
            {
              id: 'appearance',
              component: {
                Card: {
                  children: { explicitList: ['themeTitle', 'themeOptions'] }
                }
              }
            },
            {
              id: 'themeTitle',
              component: {
                Text: {
                  text: { literalString: '🎨 外观' },
                  size: 'large',
                  weight: 'bold'
                }
              }
            },
            {
              id: 'themeOptions',
              component: {
                Column: {
                  children: { explicitList: ['themeAuto', 'themeLight', 'themeDark'] },
                  spacing: { literalNumber: 8 }
                }
              }
            },
            {
              id: 'themeAuto',
              component: {
                Button: {
                  text: { literalString: '○ 自动' },
                  action: {
                    name: 'set_theme_auto',
                    surfaceId: 'settings',
                    context: []
                  },
                  style: {
                    backgroundColor: state.themeMode === 'auto' ? '#06b6d4' : undefined
                  }
                }
              }
            },
            {
              id: 'themeLight',
              component: {
                Button: {
                  text: { literalString: '◑ 浅色' },
                  action: {
                    name: 'set_theme_light',
                    surfaceId: 'settings',
                    context: []
                  },
                  style: {
                    backgroundColor: state.themeMode === 'light' ? '#06b6d4' : undefined
                  }
                }
              }
            },
            {
              id: 'themeDark',
              component: {
                Button: {
                  text: { literalString: '◐ 深色' },
                  action: {
                    name: 'set_theme_dark',
                    surfaceId: 'settings',
                    context: []
                  },
                  style: {
                    backgroundColor: state.themeMode === 'dark' ? '#06b6d4' : undefined
                  }
                }
              }
            },
            // Location section
            {
              id: 'location',
              component: {
                Card: {
                  children: { explicitList: ['locationTitle', 'locationPermission', 'locationCurrent'] }
                }
              }
            },
            {
              id: 'locationTitle',
              component: {
                Text: {
                  text: { literalString: '📍 位置服务' },
                  size: 'large',
                  weight: 'bold'
                }
              }
            },
            {
              id: 'locationPermission',
              component: {
                Text: {
                  text: { literalString: `位置权限: ${state.locationPermission === 'granted' ? '✅ 已授权' : state.locationPermission === 'denied' ? '❌ 已拒绝' : '⚠️ 未授权'}` },
                  size: 'small',
                  usageHint: 'caption'
                }
              }
            },
            {
              id: 'authorizeLocation',
              component: {
                Button: {
                  text: { literalString: '授权位置' },
                  action: {
                    name: 'authorize_location',
                    surfaceId: 'settings',
                    context: []
                  },
                  disabled: state.locationPermission !== 'prompt'
                }
              }
            },
            {
              id: 'locationCurrent',
              component: {
                Text: {
                  text: { literalString: state.currentLocation ? `纬度: ${state.currentLocation.latitude}, 经度: ${state.currentLocation.longitude}` : '位置获取中...' }
                },
                size: 'small',
                usageHint: 'caption'
              }
              }
            },
            // Voice section
            {
              id: 'voice',
              component: {
                Card: {
                  children: { explicitList: ['voiceTitle', 'voicePermission'] }
                }
              }
            },
            {
              id: 'voiceTitle',
              component: {
                Text: {
                  text: { literalString: '🎤 语音' },
                  size: 'large',
                  weight: 'bold'
                }
              }
            },
            {
              id: 'voicePermission',
              component: {
                Text: {
                  text: { literalString: `语音权限: ${state.voiceEnabled ? '✅ 已授权' : '❌ 未授权'}` },
                  size: 'small',
                  usageHint: 'caption'
                }
              }
            },
            {
              id: 'authorizeVoice',
              component: {
                Button: {
                  text: { literalString: '授权麦克风' },
                  action: {
                    name: 'authorize_voice',
                    surfaceId: 'settings',
                    context: []
                  }
                }
              }
            },
            // Advanced section
            {
              id: 'advanced',
              component: {
                Card: {
                  children: { explicitList: ['preventSleep', 'cache', 'version'] }
                }
              }
            },
            {
              id: 'preventSleep',
              component: {
                Row: {
                  children: { explicitList: ['preventSleepLabel', 'preventSleepToggle'] },
                  spacing: { literalNumber: 8 }
                }
              }
            },
            {
              id: 'preventSleepLabel',
              component: {
                Text: {
                  text: { literalString: '防止休眠:' }
                }
              }
              },
            },
            {
              id: 'preventSleepToggle',
              component: {
                Button: {
                  text: { literalString: state.preventSleep ? '已启用' : '已关闭' },
                  action: {
                    name: 'toggle_prevent_sleep',
                    surfaceId: 'settings',
                    context: []
                  },
                  style: {
                    backgroundColor: state.preventSleep ? '#06b6d4' : undefined
                  }
                }
              }
            },
            {
              id: 'cache',
              component: {
                Column: {
                  children: { explicitList: ['cacheInfo', 'clearCache'] },
                  spacing: { literalNumber: 8 }
                }
              }
            },
            {
              id: 'cacheInfo',
              component: {
                Text: {
                  text: { literalString: '存储空间: 使用中 (计算中...)' },
                  size: 'small',
                  usageHint: 'caption'
                }
              }
            },
            {
              id: 'clearCache',
              component: {
                Button: {
                  text: { literalString: '清除缓存' },
                  action: {
                    name: 'clear_cache',
                    surfaceId: 'settings',
                    context: []
                  }
                }
              },
            {
              id: 'version',
              component: {
                Text: {
                  text: { literalString: '版本: v1.0.0 (build 2025-02-04)' },
                  size: 'small',
                  usageHint: 'caption'
                }
              }
            }
          ]
        }
      }
    }
  ];

  return messages;
};
```

### 2. 聊天页面实现

#### 文件结构

```
src/canvas-host/a2ui-react/
├── pages/
│   └── chat/
│       ├── ChatPage.tsx               # 聊天页面主组件
│       ├── chatSlice.tsx             # Redux slice
│       └── ChatA2ui.tsx              # A2UI 组件
├── services/
│   ├── ChatService.ts              # 聊天服务
│   ├── AttachmentService.ts        # 附件服务
│   └── VoiceInputService.ts        # 语音输入服务
└── hooks/
    ├── useChat.ts                   # 聊天 hook
    └── useVoiceInput.ts             # 语音输入 hook
```

#### 聊天页面组件

```typescript
// src/canvas-host/a2ui-react/pages/chat/ChatPage.tsx
import React, { useState, useRef, useEffect } from 'react';
import { useChat } from '../../hooks/useChat';
import { useVoiceInput } from '../../hooks/useVoiceInput';
import { A2uiColumn } from '../../components/elements/A2uiColumn';
import { A2uiRow } from '../../components/elements/A2uiRow';
import { A2uiText } from '../../components/elements/A2uiText';
import { A2uiCard } from '../../components/elements/A2uiCard';
import { A2uiButton } from '../../components/elements/A2uiButton';
import { A2uiTextField } from '../../components/elements/A2uiTextField';
import { A2uiContainer } from '../../components/elements/A2uiContainer';
import { A2uiImage } from '../../components/elements/A2uiImage';
import { A2uiDivider } from '../../components/elements/A2uiDivider';

interface ChatMessage {
  id: string;
  role: 'user' | 'assistant' | 'system';
  content: ChatContent[];
  timestamp: number;
  attachments?: Attachment[];
  status?: 'sending' | 'sent' | 'error';
  error?: string;
}

interface ChatContent {
  type: 'text' | 'image' | 'location' | 'file' | 'link';
  text?: string;
  url?: string;
  fileName?: string;
  base64?: string;
  latitude?: number;
  longitude?: number;
  accuracy?: number;
}

export const ChatPage: React.FC = () => {
  const {
    messages,
    sendMessage,
    sendAttachment,
    inputMode,
    setInputMode
  } = useChat();

  const {
    isListening,
    startListening,
    stopListening,
    transcript
  } = useVoiceInput();

  const [inputText, setInputText] = useState('');

  // 生成聊天页面的 A2UI 消息
  const generateChatMessages = (): A2uiMessage[] => {
    const messages = [...useChat().messages];

    // 转换为 A2UI 格式
    const a2uiMessages: A2uiMessage[] = [];
    let componentId = 100;

    // 消息列表
    const messageListComponent: A2uiComponent[] = messages.map((msg) => {
      const contentComponents: A2uiComponent[] = msg.content.map((content) => {
        switch (content.type) {
          case 'text':
            return {
              id: `msg_${msg.id}_text`,
              component: {
                Text: {
                  text: { literalString: content.text || '' },
                  size: 'medium'
                }
              }
            };

          case 'image':
            return {
              id: `msg_${msg.id}_image`,
              component: {
                Image: {
                  url: { literalString: content.url || '' },
                  style: { width: '100%', height: 'auto' }
                }
              }
            };

          case 'location':
            return {
              id: `msg_${msg.id}_location`,
              component: {
                Container: {
                  padding: { literalNumber: 12 },
                  style: {
                    backgroundColor: '#e3f2fd',
                    borderRadius: '8px'
                  },
                  children: { explicitList: ['locationIcon', 'locationText'] }
                }
              }
            };

          case 'file':
            return {
              id: `msg_${msg.id}_file`,
              component: {
                Container: {
                  padding: { literalNumber: 12 },
                  style: {
                    backgroundColor: '#f0f0f0',
                    borderRadius: '8px'
                  },
                  children: { explicitList: ['fileIcon', 'fileName'] }
                }
              }
            };

          default:
            return null;
        }
      }).filter(Boolean) as A2uiComponent[];

      return {
        id: `msg_${msg.id}`,
        component: {
          Container: {
            style: {
              marginBottom: { literalNumber: 8 },
              backgroundColor: msg.role === 'user' ? '#f0f9ff' : undefined,
              borderRadius: '8px',
              padding: { literalNumber: 12 }
            },
            children: contentComponents
          }
        }
      };
    });

    // 输入框
    const inputComponent: A2uiComponent = {
      id: 'input',
      component: {
        Row: {
          children: { explicitList: ['quickActions', 'input', 'sendButton'] },
          spacing: { literalNumber: 8 }
        }
      }
    };

    componentId += messageListComponent.length + 1;

    // 快捷操作按钮
    const quickActions: A2uiComponent = {
      id: 'quickActions',
      component: {
        Row: {
          children: { explicitList: [] }
        }
      }
    };

    // 输入框
    const inputComponent: A2uiComponent = {
      id: 'input',
      component: {
        TextField: {
          placeholder: { literalString: '输入消息...' },
          value: { literalString: inputText },
          style: {
            flex: 1
          },
          action: {
            name: 'update_input_text',
            surfaceId: 'chat',
            context: []
          }
        }
      }
    };

    // 发送按钮
    const sendButtonComponent: A2uiComponent = {
      id: 'sendButton',
      component: {
        Button: {
          text: { literalString: '发送' },
          action: {
            name: 'send_message',
            surfaceId: 'chat',
            context: []
          },
          disabled: !inputText.trim()
        }
      }
    };

    // A2UI Root Message
    return [
      {
        surfaceUpdate: {
          surfaceId: 'chat',
          components: [
            {
              id: 'root',
              component: {
                Column: {
                  children: { explicitList: [...messageListComponent, 'input'] },
                  spacing: { literalNumber: 12 }
                }
              }
            }
          }
        }
      },
      {
        beginRendering: {
          surfaceId: 'chat',
          root: 'root'
        }
      }
    ];
  };
```

### 3. 语音助手页面实现

#### UI 组件

```typescript
// src/canvas-host/a2ui-react/pages/voice/VoiceAssistantPage.tsx
import React, { useState, useRef, useEffect } from 'react';
import { useVoiceAssistant } from '../../hooks/useVoiceAssistant';
import { A2uiColumn } from '../../components/elements/A2uiColumn';
import { A2uiRow } from '../../components/elements/A2uiRow';
import { A2uiText } from '../../components/elements/A2uiText';
import { A2uiCard } from '../../components/elements/A2uiCard';
import { A2uiButton } from '../../components/elements/A2uiButton';
import { A2uiContainer } from '../../components/elements/A2uiContainer';
import { A2uiDivider } from '../../components/elements/A2uiDivider';

interface VoiceAssistantState {
  isListening: boolean;
  transcript: string;
  aiResponse: string;
  isProcessing: boolean;
}

export const VoiceAssistantPage: React.FC = () => {
  const {
    isListening,
    startListening,
    stopListening,
    transcript,
    aiResponse,
    isProcessing
  } = useVoiceAssistant();

  return (
    <A2uiColumn spacing={{ literalNumber: 16 }}>
      {/* 状态指示器 */}
      <A2uiCard>
        <A2uiText>
          {isListening ? '🎙 正在听...' : '🎤 语音助手'}
        </A2iText>
        {isListening && (
          <A2uiText size="small" usageHint="caption">
            {transcript || '正在监听...'}
          </A2iText>
        )}
        {!isListening && aiResponse && (
          <A2iText size="small" usageHint="caption">
            {aiResponse}
          </A2iText>
        )}
      </A2uiCard>

      {/* 对话历史 */}
      <A2uiColumn spacing={{ literalNumber: 12 }}>
        {isListening && (
          <A2uiCard>
            <A2iText>💬 对话中...</A2iText>
            <A2uiText size="small" usageHint="caption">
              请说话...
            </A2iText>
          </A2uiCard>
        )}

        {!isListening && aiResponse && (
          <A2uiCard>
            <A2uiText>🤖 AI 回复:</A2iText>
            <A2iText>{aiResponse}</A2iText>
          </A2iCard>
        )}
      </A2uiColumn>

      {/* 操作按钮 */}
      <A2uiRow spacing={{ literalNumber: 8 }}>
        <A2uiButton
          text={isListening ? '停止' : '开始'}
          action={{
            name: isListening ? 'stop_listening' : 'start_listening',
            surfaceId: 'voice',
            context: []
          }}
        />
        <A2uiButton
          text="返回"
          action={{
            name: 'go_to_chat',
            surfaceId: 'voice',
            context: []
          }}
        />
      </A2uiRow>
    </A2uiColumn>
  );
};
```

---

## 数据流设计

### 1. WebSocket 通信协议

#### 消息类型定义

```typescript
// src/canvas-host/a2ui-react/types/protocol.ts

/**
 * Gateway 通信协议
 */
export interface GatewayMessage {
  type: 'chat' | 'a2ui' | 'action' | 'voice' | 'location';
  payload: any;
  timestamp: number;
  sessionId?: string;
}

/**
 * 聊天消息
 */
export interface ChatMessage {
  sessionId: string;
  type: 'user' | 'assistant' | 'system';
  content: string;
  attachments?: Attachment[];
  timestamp: number;
}

/**
 * 附件
 */
export interface Attachment {
  type: 'image' | 'location' | 'file' | 'audio' | 'link';
  data: any;
}

/**
 * 语音消息
 */
export interface VoiceMessage {
  sessionId: string;
  audio: {
    format: 'mp3' | 'wav' | 'opus';
    sampleRate: number;
    channelCount: number;
    data: string; // base64
  };
  timestamp: number;
}

/**
 * 位置消息
 */
export interface LocationMessage {
  sessionId: string;
  location: {
    latitude: number;
    longitude: number;
    accuracy: number;
    altitude?: number;
    speed?: number;
  };
  timestamp: number;
}
```

### 2. WebSocket 连接管理

```typescript
// src/canvas-host/a2ui-react/services/GatewayService.ts

/**
 * Gateway WebSocket 服务
 */
export class GatewayService {
  private ws: WebSocket | null = null;
  private reconnectTimer: NodeJS.Timeout | null = null;
  private reconnectDelay = 1000; // 1s
  private maxReconnectDelay = 30000; // 30s
  private messageQueue: GatewayMessage[] = [];

  constructor(private url: string) {
    this.connect();
  }

  /**
   * 连接到 Gateway
   */
  private connect(): void {
    this.ws = new WebSocket(this.url);

    this.ws.onopen = () => {
      console.log('[Gateway] WebSocket 已连接');
      this.reconnectDelay = 1000;
      this.flushMessageQueue();
    };

    this.ws.onmessage = (event: MessageEvent) => {
      this.handleMessage(event.data);
    };

    this.ws.onerror = (error) => {
      console.error('[Gateway] WebSocket 错误:', error);
    };

    this.ws.onclose = () => {
      console.log('[Gateway] WebSocket 已关闭');
      this.scheduleReconnect();
    };
  }

  /**
   * 发送消息到 Gateway
   */
  send(message: GatewayMessage): void {
    if (this.ws?.readyState === WebSocket.OPEN) {
      this.ws.send(JSON.stringify(message));
    } else {
      this.messageQueue.push(message);
    }
  }

  /**
   * 处理来自 Gateway 的消息
   */
  private handleMessage(data: string): void {
    try {
      const message = JSON.parse(data) as GatewayMessage;

      switch (message.type) {
        case 'chat':
          this.handleChatMessage(message);
          break;
        case 'a2ui':
          this.handleA2uiMessage(message);
          break;
        case 'voice':
          this.handleVoiceMessage(message);
          break;
        case 'action':
          this.handleActionMessage(message);
          break;
      }
    } catch (error) {
      console.error('[Gateway] 消息解析错误:', error);
    }
  }

  /**
   * 处理聊天消息
   */
  private handleChatMessage(message: any): void {
    // 更新聊天状态
    // 触发重新渲染
  }

  /**
   * 处理 A2UI 消息
   */
  private handleA2uiMessage(message: any): void {
    // 更新 A2UI Canvas
    if (window.openclawA2UI) {
      window.openclawA2UI.applyMessages(message.payload);
    }
  }

  /**
   * 自动重连
   */
  private scheduleReconnect(): void {
    if (this.reconnectTimer) return;

    this.reconnectTimer = setTimeout(() => {
      console.log('[Gateway] 重连中...');
      this.connect();

      if (this.ws?.readyState !== WebSocket.OPEN) {
        this.reconnectDelay = Math.min(this.reconnectDelay * 2, this.maxReconnectDelay);
        this.scheduleReconnect();
      } else {
        this.reconnectTimer = null;
      }
    }, this.reconnectDelay);
  }
}
```

### 3. 语音输入服务

```typescript
// src/canvas-host/a2ui-react/services/VoiceInputService.ts

/**
 * 语音输入服务
 * 使用 Web Audio API + 云 ASR
 */
export class VoiceInputService {
  private mediaRecorder: MediaRecorder | null = null;
  private audioContext: AudioContext | null = null;
  private recognitionUrl: string;

  constructor(recognitionUrl: string) {
    this.recognitionUrl = recognitionUrl;
  }

  /**
   * 开始录音
   */
  async startRecording(): Promise<void> {
    // 请求麦克风权限
    const stream = await navigator.mediaDevices.getUserMedia({
      audio: {
        echoCancellation: true,
        noiseSuppression: true,
        sampleRate: 16000,
        channelCount: 1
      }
    });

    this.audioContext = new AudioContext({ sampleRate: 16000 });

    // 创建录音器
    this.mediaRecorder = new MediaRecorder(stream, {
      mimeType: 'audio/webm',
      audioBitsPerSecond: 16000
    });

    // 创建 ScriptProcessor 用于实时转录
    const processor = this.audioContext.createScriptProcessor(4096, 1, 1);

    processor.onaudioprocess = (e) => {
      const inputData = e.inputBuffer.getChannelData(0);

      // 转换为 Int16
      const int16Data = new Int16Array(inputData.length);
      for (let i = 0; i < inputData.length; i++) {
        const s = Math.max(-1, Math.min(1, inputData[i]));
        int16Data[i] = s < 0 ? s * 0x8000 : s * 0x7FFF;
      }

      // 发送到云 ASR 服务
      this.sendAudioToCloudASR(int16Data.buffer);
    };

    const source = this.audioContext.createMediaStreamSource(stream);
    source.connect(processor);
    processor.connect(this.audioContext.destination);

    this.mediaRecorder.start();
  }

  /**
   * 停止录音
   */
  async stopRecording(): Promise<string> {
    return new Promise((resolve) => {
      if (this.mediaRecorder?.state === 'recording') {
        this.mediaRecorder.ondataavailable = async (e) => {
          resolve('录音完成');
        };
        this.mediaRecorder.stop();
      } else {
        resolve('未在录音');
      }
    });
  }

  /**
   * 发送音频到云 ASR
   */
  private sendAudioToCloudASR(audioData: ArrayBuffer): void {
    // 转换为 base64
    const base64 = this.arrayBufferToBase64(audioData);

    // 通过 WebSocket 发送到 Gateway
    if (window.openclawGateway) {
      window.openclawGateway.send({
        type: 'voice',
        payload: {
          audio: {
            format: 'mp3',
            sampleRate: 16000,
            channelCount: 1,
            data: base64
          },
          timestamp: Date.now()
        }
      } as GatewayMessage);
    }
  }

  private arrayBufferToBase64(buffer: ArrayBuffer): string {
    const bytes = new Uint8Array(buffer);
    let binary = '';
    for (let i = 0; i < bytes.length; i++) {
      binary += String.fromCharCode(bytes[i]);
    }
    return btoa(binary);
  }
}
```

---

## A2UI 消息定义

### 设置页面 A2UI 消息

```json
{
  "surfaceUpdate": {
    "surfaceId": "settings",
    "components": [
      {
        "id": "root",
        "component": {
          "Column": {
            "children": { "explicitList": ["header", "sections", "actions"] },
            "spacing": { "literalNumber": 16 }
          }
        }
      },
      {
        "id": "header",
        "component": {
          "Text": {
            "text": { "literalString": "⚙️ 设置" },
            "size": "xlarge",
            "weight": "bold"
          }
        }
      },
      {
        "id": "sections",
        "component": {
          "Column": {
            "children": { "explicitList": [
              {
                "id": "deviceSection",
                "component": {
                  "Card": {
                    "children": { "explicitList": ["deviceName", "deviceId"] }
                  }
                }
              },
              {
                "id": "deviceName",
                "component": {
                  "TextField": {
                    "label": { "literalString": "设备名称" },
                    "value": { "literalString": "我的 H5" },
                    "placeholder": { "literalString": "我的 Android" }
                  }
                }
              },
              {
                "id": "deviceId",
                "component": {
                  "Text": {
                    "text": { "literalString": "设备 ID: abc-123-def" },
                    "size": "small",
                    "usageHint": "caption"
                  }
                }
              },
              {
                "id": "gatewaySection",
                "component": {
                  "Card": {
                    "children": { "explicitList": ["status", "serverName", "remoteAddress", "actions"] }
                  }
                }
              },
              {
                "id": "status",
                "component": {
                  "Text": {
                    "text": { "literalString": "连接状态: ● 已连接" },
                    "size": "medium"
                  }
                }
              },
              {
                "id": "actions",
                "component": {
                  "Row": {
                    "children": { "explicitList": ["refresh", "disconnect"] },
                    "spacing": { "literalNumber": 8 }
                  }
                }
              }
            ]
          }
        }
      }
    ]
  }
}
```

---

## 实施计划

### Week 1: 基础架构（3-5 天）

#### Day 1-2: 项目初始化
- [ ] 创建页面目录结构
- [ ] 设置基础样式和组件
- [ ] A2UI 组件扩展（如果需要）

#### Day 3-5: 核心功能
- [ ] 设置页面 + 服务
- [ ] 聊天页面 + 服务
- [ ] WebSocket 连接管理
- [ ] 状态管理（Context + Hooks）

### Week 2: 多媒体功能（5-7 天）

#### Day 1-2: 相机功能
- [ ] H5CameraService
- [ ] 拍照 UI 组件
- [ ] 图片预览
- [ ] Base64 编码

#### Day 3-4: 屏幕录制
- [ ] H5ScreenRecorderService
- [ ] 录制状态指示
- [ ] 视频 Blob 处理

#### Day 5-7: 位置服务
- [ ] H5LocationService
- [ ] 地理位置权限处理
- [ ] 位置显示组件

### Week 3: 语音功能（5-7 天）

#### Day 1-3: 语音输入
- [ ] H5VoiceInputService
- [ ] Web Audio API 集成
- [ ] 云 ASR 集成

#### Day 4-5: 语音输出
- [ ] 云 TTS 集成
- [ ] 语音播放 UI

#### Day 6-7: 语音助手页面
- [ ] 对话 UI
- [ ] 语音输入/输出集成
- [ ] 语音状态指示

### Week 4: 测试与优化（3-5 天）

- [ ] 功能测试
- [ ] 性能优化
- [ ] 兼容性测试
- [ ] Bug 修复

---

## 关键代码示例

### 1. 设置服务

```typescript
// src/canvas-host/a2ui-react/services/SettingsService.ts

/**
 * 设置服务
 */
export class SettingsService {
  /**
   * 获取设置
   */
  async getSettings(): Promise<SettingsState> {
    const response = await fetch('/api/settings');
    return await response.json();
  }

  /**
   * 更新设置
   */
  async updateSettings(settings: Partial<SettingsState>): Promise<void> {
    await fetch('/api/settings', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(settings)
    });
  }

  /**
   * 连接到 Gateway
   */
  async connectGateway(gatewayId: string): Promise<void> {
    await fetch('/api/gateway/connect', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ gatewayId })
    });
  }

  /**
   * 断开 Gateway
   */
  async disconnectGateway(): Promise<void> {
    await fetch('/api/gateway/disconnect', {
      method: 'POST'
    });
  }
}
```

### 2. 聊天服务

```typescript
// src/canvas-host/a2ui-react/services/ChatService.ts

/**
 * 聊天服务
 */
export class ChatService {
  private sessionId = 'main';
  private ws: WebSocket | null = null;

  /**
   * 连接到 Gateway 聊天服务
   */
  async connect(sessionId?: string): Promise<void> {
    this.sessionId = sessionId || 'main';
    const wsUrl = `wss://gateway.example.com/chat/${this.sessionId}`;

    this.ws = new WebSocket(wsUrl);

    this.ws.onmessage = (event) => {
      const message = JSON.parse(event.data) as ChatMessage;
      this.handleMessage(message);
    };
  }

  /**
   * 发送文本消息
   */
  async sendText(text: string): Promise<void> {
    const message: ChatMessage = {
      sessionId: this.sessionId,
      type: 'user',
      content: text,
      timestamp: Date.now()
    };

    this.ws?.send(JSON.stringify(message));
  }

  /**
   * 发送附件
   */
  async sendAttachment(attachment: Attachment): Promise<void> {
    const message: ChatMessage = {
      sessionId: this.sessionId,
      type: 'user',
      content: '',
      attachments: [attachment],
      timestamp: Date.now()
    };

    this.ws?.send(JSON.stringify(message));
  }

  /**
   * 处理收到的消息
   */
  private handleMessage(message: ChatMessage): void {
    // 更新聊天状态
    // 触发重新渲染
  }
}
```

### 3. 语音输入服务

```typescript
// src/class-host/a2ui-react/services/VoiceInputService.ts

/**
 * 语音输入服务
 */
export class VoiceInputService {
  private recorder: MediaRecorder | null = null;
  private recognitionUrl = 'https://gateway.example.com/asr';

  /**
   * 开始录音
   */
  async startRecording(): Promise<void> {
    const stream = await navigator.mediaDevices.getUserMedia({ audio: true });

    this.recorderer = new MediaRecorder(stream);

    // 实时转录
    this.recorderer.ondataavailable = async (event) => {
      const audioBlob = event.data;
      await this.sendToASR(audioBlob);
    };

    this.recorderer.start();
  }

  /**
   * 停止录音
   */
  async stopRecording(): Promise<void> {
    return new Promise((resolve) => {
      if (this.recorderer?.state === 'recording') {
        this.recorderer.ondataavailable = async (event) => {
          resolve('录音完成');
        };
        this.recorderer.stop();
      } else {
        resolve('未在录音');
      }
    });
  }

  /**
   * 发送音频到 ASR 服务
   */
  private async sendToASR(audioBlob: Blob): Promise<string> {
    const formData = new FormData();
    formData.append('audio', audioBlob, 'audio.mp3');
    formData.append('format', 'mp3');

    const response = await fetch(this.recognitionUrl, {
      method: 'POST',
      body: formData
    });

    const data = await response.json();
    return data.transcript;
  }
}
```

---

## 总结

### 核心价值

**纯 H5 方案的优势**：
1. ✅ **复用已有**：A2UI React H5 已完成
2. ✅ **快速开发**：无审核限制，即时更新
3. ✅ **跨平台**：所有现代浏览器都支持
4. ✅ **功能完整**：支持相机、定位、屏幕录制

**功能覆盖**：
- ✅ Canvas UI (100%)
- ✅ Gateway 连接 (100%)
- ✅ 相机拍照 (100%)
- ✅ 屏幕录制 (100%)
- ✅ 位置服务 (70% - 无后台)
- ✅ 聊天功能 (100%)
- ✅ 语音输入 (90% - 无后台监听)
- ✅ 语音输出 (95%)

**限制**：
- ❌ 后台运行（标签页关闭即停止）
- ❌ 短信（需要云服务）
- ⚠️ 推送通知（Service Worker 有限）

**适用场景**：
- ✅ 临时使用
- ✅ 功能演示
- ✅ 技术支持
- ✅ 用户习惯使用浏览器的场景

---

**文档版本**: v1.0
**创建日期**: 2025-02-04
**作者**: OpenClaw Team
